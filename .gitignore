
# Built binaries
bin/
/agent-tasks
# examples/* leave these in cwd when run without -o or from the repo root.
/two-agents
/two-agents-commit
/two-agents-chaos
/.nats-store/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Read-only reference clones — nested git repos; see README §Future git init note.
# Ignore subdirectories only so our own docs (e.g. reference/CAPABILITIES.md) stay tracked.
reference/*/

# Go workspace — developer-local copy of go.work.example. Do NOT commit.
go.work
go.work.sum

# Claude Code / scheduled_tasks runtime state (local-only).
.claude/
